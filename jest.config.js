// jest.config.js
module.exports = {
  // ... other configurations
  setupFiles: ['<rootDir>/jest.setup.js'], // Adjust path if needed
  // or use setupFilesAfterEnv if you need the test framework to be set up first
};
